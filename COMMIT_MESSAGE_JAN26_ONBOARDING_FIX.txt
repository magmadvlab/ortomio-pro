fix(onboarding): Implement optimistic UI update for garden creation

CRITICAL FIX: Resolve race condition where newly created gardens don't appear in UI

Problem:
- User creates garden successfully (saved to database)
- UI immediately shows "No gardens found" again
- Race condition: getGardens() called before DB commit/RLS recognition

Solution:
- Optimistic UI update: Add garden to state immediately after creation
- Close wizard instantly for better UX
- Background refresh with retry logic (3 attempts, 200ms delay)
- Keep optimistic state if refresh fails (garden was created successfully)

Changes:
- app/app/page.tsx:
  * Add refreshGardensWithRetry() helper function
  * Modify onComplete callback to use optimistic updates
  * Add comprehensive logging for debugging
  * Implement retry logic with garden ID verification

Benefits:
- Instant UI feedback (no more "No gardens found" after creation)
- Resilient to database timing issues
- Better user experience with immediate dashboard display
- Graceful degradation if refresh fails

Testing:
- Verified garden appears immediately after creation
- Tested with network delays
- Confirmed retry logic works correctly
- Validated state consistency

Spec: .kiro/specs/onboarding-fix/
Tasks: Phase 1 (Quick Fix) - Tasks 1 & 2 complete
