fix: Complete transplant functionality - resolve 404 errors and storage issues

🎯 ISSUE RESOLVED:
- Transplant modal failing with 404 resource loading errors
- Storage provider methods not implemented for individual plants and field rows
- LocalStorageProvider had stub implementations throwing errors
- Multiple GoTrueClient instances warning (non-blocking)

🔧 TECHNICAL FIXES:

Storage Interface (packages/core/storage/interface.ts):
- Added individual plant methods to IStorageProvider interface:
  * getIndividualPlants?(gardenId: string)
  * createIndividualPlant?(plant)
  * updateIndividualPlant?(id, updates)
  * deleteIndividualPlant?(id)

LocalStorageProvider (packages/storage-local/LocalStorageProvider.ts):
- Implemented full individual plant CRUD operations using localStorage
- Fixed field row methods (were stub implementations throwing errors)
- Added proper error handling and console logging
- Uses garden-scoped storage keys: ortoIndividualPlants_${gardenId}

SupabaseStorageProvider (packages/storage-cloud/SupabaseStorageProvider.ts):
- Added individual plant methods with proper database mapping
- Implements full CRUD operations on 'individual_plants' table
- Proper data transformation between app types and DB schema
- Error handling for database operations

🌱 TRANSPLANT WORKFLOW NOW WORKS:
1. User clicks "Trapianta nell'Orto" → Modal opens successfully
2. Modal loads field rows → getFieldRows() works (no more 404)
3. User configures transplant → All validation works
4. Execute transplant → Individual plants created successfully
5. Save to database → createIndividualPlant() works (no more 404)
6. Update field row → updateFieldRow() works (no more 404)
7. Success message → Complete workflow functional

🐛 DEBUG IMPROVEMENTS:
- Enhanced console logging throughout transplant process
- Better error messages with technical details
- Step-by-step logging for troubleshooting
- Storage operation confirmations

📱 USER EXPERIENCE:
- No more 404 errors during transplant
- Individual plants properly saved and trackable
- Field rows updated with transplant information
- Orchestrator activated for automatic monitoring
- Plants visible in "Ispeziona Piante" page
- Complete vivaio → orto → monitoring workflow

✅ All storage provider methods now properly implemented for both
   localStorage and Supabase, enabling full transplant functionality.